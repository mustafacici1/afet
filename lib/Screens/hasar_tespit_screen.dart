import 'package:flutter/material.dart';

class HasarTespitScreen extends StatelessWidget {
  const HasarTespitScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Hasar Tespit Ekranı'),
    );
  }
}
